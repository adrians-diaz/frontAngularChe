export interface Objetos {
}

export interface mesasInterface {
    id: string;
    name: string;
    zona: string;
    comensales: number;
}

export interface cartasInterface {
    id: string,
	producto: string,
	descripcion: string,
	precio: number,
	tipo: string,
	imagen: string,
}
